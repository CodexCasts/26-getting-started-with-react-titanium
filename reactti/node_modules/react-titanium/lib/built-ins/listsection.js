'use strict';

var _ReactTitaniumBridge = require('../ReactTitaniumBridge');

(0, _ReactTitaniumBridge.register)('listsection', 'Ti.UI.ListSection', {
  factory: function factory(props) {
    return Ti.UI.createListSection(props);
  },

  create: function create(props, handlers, getChildren) {
    return this.factory(props);
  }
});