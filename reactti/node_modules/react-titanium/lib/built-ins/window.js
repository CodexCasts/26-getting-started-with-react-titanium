"use strict";

var _ReactTitaniumBridge = require('../ReactTitaniumBridge');

(0, _ReactTitaniumBridge.register)("window", "Ti.UI.Window", {
  factory: function factory(props) {
    return Ti.UI.createWindow(props);
  }
});